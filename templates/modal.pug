input(type="checkbox" id="modal-toggle" class="modal-toggle")
.modal
  label(for="modal-toggle" class="modal-overlay")
  .modal-content
    .modal-header
      h3.modal-title Confirmation
      label(for="modal-toggle" class="modal-close")
        svg.modal-close-icon(viewBox="0 0 24 24" fill="none")
          path(d="M18 6L6 18M6 6l12 12")
    .modal-body
      p Are you sure you want to delete this item? This action cannot be undone.
    .modal-footer
      label(for="modal-toggle" class="btn btn-secondary") Cancel
      button.btn.btn-danger Delete Item
